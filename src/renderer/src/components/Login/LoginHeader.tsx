import { Center, Title } from "@mantine/core";

const LoginHeader = () => (
  <Center>
    <Title ta="center" order={2} mb="lg">
      Iniciar Sesión
    </Title>
  </Center>
);

export default LoginHeader;